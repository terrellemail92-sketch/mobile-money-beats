{
  "name": "mobile-money-beats",
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev-backend": "npm run dev --prefix backend"
  },
  "dependencies": {
    "stripe": "^17.7.0",
    "resend": "^4.6.0"
  },
  "devDependencies": {
    "concurrently": "latest"
  }
}
